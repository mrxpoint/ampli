import * as React from "react";

import {
  Create,
  SimpleForm,
  CreateProps,
  TextInput,
  PasswordInput,
  SelectArrayInput,
  ReferenceArrayInput,
} from "react-admin";

import { SwapTitle } from "../swap/SwapTitle";
import { PoolTitle } from "../pool/PoolTitle";
import { PairTitle } from "../pair/PairTitle";
import { ROLES_OPTIONS } from "../user/RolesOptions";

export const UserCreate = (props: CreateProps): React.ReactElement => {
  return (
    <Create {...props}>
      <SimpleForm>
        <TextInput label="First Name" source="firstName" />
        <TextInput label="Last Name" source="lastName" />
        <TextInput label="Username" source="username" />
        <TextInput label="Email" source="email" type="email" />
        <PasswordInput label="Password" source="password" />
        <SelectArrayInput
          source="roles"
          choices={ROLES_OPTIONS}
          optionText="label"
          optionValue="value"
        />
        <TextInput label="userName" source="userName" />
        <TextInput label="walletAddress" source="walletAddress" />
        <ReferenceArrayInput
          source="swaps"
          reference="Swap"
          parse={(value: any) => value && value.map((v: any) => ({ id: v }))}
          format={(value: any) => value && value.map((v: any) => v.id)}
        >
          <SelectArrayInput optionText={SwapTitle} />
        </ReferenceArrayInput>
        <ReferenceArrayInput
          source="pools"
          reference="Pool"
          parse={(value: any) => value && value.map((v: any) => ({ id: v }))}
          format={(value: any) => value && value.map((v: any) => v.id)}
        >
          <SelectArrayInput optionText={PoolTitle} />
        </ReferenceArrayInput>
        <ReferenceArrayInput
          source="pairs"
          reference="Pair"
          parse={(value: any) => value && value.map((v: any) => ({ id: v }))}
          format={(value: any) => value && value.map((v: any) => v.id)}
        >
          <SelectArrayInput optionText={PairTitle} />
        </ReferenceArrayInput>
      </SimpleForm>
    </Create>
  );
};
